name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My first GitHub action job"

        - name: List Files
          run: ls

        - name: Read the files
          run: cat README.md
